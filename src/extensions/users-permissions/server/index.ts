// import routes from './routes';


import controllers from './controllers';
import services from './services';
import servicesz from './services';
export default {
  controllers,
  services,
  type: 'content-api', // can also be 'admin' depending on the type of route
};